/**
 * @typedef {'user' | 'environment'} CameraFacingMode
 */

/**
 * @typedef {Object} CameraStartOptions
 * @property {HTMLVideoElement} videoElement
 * @property {CameraFacingMode} [facingMode]
 * @property {string} [deviceId]
 * @property {number} [width]
 * @property {number} [height]
 */

/**
 * @typedef {'idle' | 'starting' | 'active' | 'paused'} CameraLifecycleState
 */

export {};
